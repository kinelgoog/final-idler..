services:
  - type: web
    name: steam-idler
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    plan: free
    envVars:
      - key: STEAM_LOGIN
        sync: false
      - key: STEAM_PASSWORD
        sync: false
      - key: APP_IDS
        value: "730"
